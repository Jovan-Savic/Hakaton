//GetData();
/*const express = require("express");
const app = express();
const ConnectDatabase = require("./Database/Database");
const Sport = require("./Database/Sport");

const PORT = 3000;


app.listen(PORT, () =>{
console.log("pocelo");

});

ConnectDatabase();

app.use(express.json());*/

let emil=document.querySelector(".emaill");
let sifra=document.querySelector(".passl");
if(emil.IndexOf('@'>-1))//proveras da li emil postoji
{
if(passl)//proverava da li pasvord odgovara
{
    //loguje se
}
}
else
{
    let gre=document.querySelector(".emailgreska");
    eventdiv.innerHTML="Greska sa imejlom";
}